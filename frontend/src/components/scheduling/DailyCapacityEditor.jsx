import { useState } from "react";
export default function DailyCapacityEditor({day,onSave}){const [capacity,setCapacity]=useState(day?.maxRequestCapacity??0),[reason,setReason]=useState("");if(!day)return <p>請先選擇日期。</p>;return <div className="workday-settings"><h3>每日申請容量</h3><label>容量<input type="number" min="0" value={capacity} onChange={e=>setCapacity(e.target.value)}/></label><label>調整原因<input value={reason} onChange={e=>setReason(e.target.value)} required/></label><button type="button" onClick={()=>onSave({...day,maxRequestCapacity:Number(capacity),reason})} disabled={!reason.trim()}>儲存容量</button></div>}
