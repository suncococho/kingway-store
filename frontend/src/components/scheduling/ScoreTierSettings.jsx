export default function ScoreTierSettings({rules=[],onSave}){return <section className="workday-settings"><h3>分數級距設定</h3>{rules.length?rules.map(rule=><div key={rule.id}>{rule.tier_name||rule.tierName}：每週 {rule.max_selectable_days||rule.maxSelectableDays} 天{rule.is_neutral_default?"（中性預設）":""}</div>):<p>尚未設定級距；必須保留一個中性預設。</p>}<button type="button" onClick={onSave}>編輯級距</button></section>}
