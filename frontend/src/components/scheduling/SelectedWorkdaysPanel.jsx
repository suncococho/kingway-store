export default function SelectedWorkdaysPanel({dates,onRemove,onClear}){return <aside className="workday-selected-panel"><div><h3>已選工作日</h3><button type="button" onClick={onClear} disabled={!dates.length}>全部清除</button></div>{dates.length?<div className="workday-chips">{dates.map(date=><button type="button" key={date} onClick={()=>onRemove(date)} aria-label={`移除 ${date}`}>{date} ×</button>)}</div>:<p>點選月曆日期即可加入。</p>}</aside>}
